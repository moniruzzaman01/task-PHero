import React from "react";

const Loading = () => {
  return (
    <div className=" absolute top-0 left-0 min-h-screen w-full bg-gray-50 bg-opacity-50 flex items-center justify-center">
      <h2 className=" text-2xl text-black font-bold">Loading ...</h2>
    </div>
  );
};

export default Loading;
